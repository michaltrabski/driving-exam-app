import { storage } from "../../functions/functions";

export const usersReducer = (state = {}, actions) => {
  switch (actions.type) {
    case "XXXXXXXXXXX":
      return state;
    //------------------------------------------------------------
    default:
      return state;
  }
};
